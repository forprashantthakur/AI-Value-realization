services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: avp
      POSTGRES_PASSWORD: avp
      POSTGRES_DB: avp
    ports: ["5432:5432"]
    volumes: [avp-data:/var/lib/postgresql/data]
volumes:
  avp-data:
